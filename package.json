{
  "name": "cerebral",
  "engines": {
    "node": "6.3.1"
  },
  "version": "1.0.0",
  "description": "flashcard app",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "test",
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/savyounts/cerebral.git"
  },
  "author": "sav younts",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/savyounts/cerebral/issues"
  },
  "homepage": "https://github.com/savyounts/cerebral#readme"
}
